[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "auto-job-hunting-agent"
version = "0.1.0"
description = "RAG-backed job hunt agent with LangChain, Selenium, and Streamlit"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
